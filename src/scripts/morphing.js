// ============================================================
// morphing.js — Ya no se usa morphing de paneles en la nueva version
// Se mantiene por compatibilidad
// ============================================================
export function iniciarMorphing() {
  // Sin efecto en la version actual
}
